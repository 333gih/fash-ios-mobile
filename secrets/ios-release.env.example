# Copy to secrets/ios-release.env (gitignored) and fill in values.
# Do NOT commit ios-release.env or binary files (.p12, .mobileprovision, .p8).
#
# Upload to GitHub Actions (repo fashandcurious14052026-dotcom/fash-ios-mobile):
#   Windows:  .\scripts\push_github_ios_secrets.ps1 -Repo fashandcurious14052026-dotcom/fash-ios-mobile
#   Mac/Linux: ./scripts/push_github_ios_secrets.sh
#
# Requires: GitHub CLI (`gh auth login`) with repo admin.

# --- Apple Developer (bắt buộc cho iOS Release) ---
APPLE_TEAM_ID=

# Mật khẩu khi export file .p12
IOS_DISTRIBUTION_CERTIFICATE_PASSWORD=

# Tên provisioning profile (App Store) — đúng như trong Apple Developer → Profiles
# Ví dụ: Fash App Store
IOS_PROVISIONING_PROFILE_SPECIFIER=

# Đường dẫn file (tương đối repo) — script tự encode base64 lên GitHub
IOS_DISTRIBUTION_CERTIFICATE_PATH=secrets/AppleDistribution.p12
IOS_PROVISIONING_PROFILE_PATH=secrets/Fash_AppStore.mobileprovision

# Hoặc dán base64 sẵn (bỏ qua PATH ở trên nếu dùng 2 dòng này):
# IOS_DISTRIBUTION_CERTIFICATE_BASE64=
# IOS_PROVISIONING_PROFILE_BASE64=

# --- App Store Connect / TestFlight (tuỳ chọn) ---
APP_STORE_CONNECT_ISSUER_ID=
APP_STORE_CONNECT_API_KEY_ID=

# Đường dẫn file .p8 (khuyến nghị) — script đọc nội dung multiline
APP_STORE_CONNECT_API_PRIVATE_KEY_PATH=secrets/AuthKey_XXXXXXXXXX.p8

# Hoặc dán nguyên private key (giữ BEGIN/END), dùng dấu ngoặc kép nếu một dòng:
# APP_STORE_CONNECT_API_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# --- Firebase FCM + Google Sign-In (TestFlight — Fash-Prod) ---
# Tải từ Firebase Console SAU KHI bật Authentication → Google cho app iOS.
# Plist PHẢI có CLIENT_ID + REVERSED_CLIENT_ID (không chỉ GOOGLE_APP_ID).
GOOGLE_SERVICE_INFO_PLIST_PATH=secrets/GoogleService-Info.plist
# Hoặc dán base64 sẵn (bỏ qua PATH nếu dùng dòng này):
# GOOGLE_SERVICE_INFO_PLIST_BASE64=

# --- Google Sign-In iOS OAuth client (tuỳ chọn nếu plist chưa có CLIENT_ID) ---
# GCP → Credentials → iOS client (bundle com.pc.fash-ios-mobile). Pushed as GitHub secret GOOGLE_IOS_CLIENT_ID.
# GOOGLE_IOS_CLIENT_ID=
