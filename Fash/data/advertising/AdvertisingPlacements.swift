import Foundation

/// CMS placement keys — must match core-service `app_ad_placements.placement_key`.
enum AdvertisingPlacements {
    static let promoSliderMain = "promo_slider_main"
    static let loginSlider = "login_slider"
    static let guestHomeBanner = "guest_home_banner"
    static let guestReengagement = "guest_reengagement"
}
